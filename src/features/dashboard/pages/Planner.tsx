type Props = {};

const Planner = (props: Props) => {
    return (
        <ul>
            <li> - Plan future transactions (weekly, monthly, yearly)</li>
            <li> - Complete transactions using the planned list</li>
        </ul>
    );
};

export default Planner;
